import styled from "styled-components";

export const CatContentWrapper = styled.div`
    >.my-top {
        padding: 10px 0;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        border-bottom: 1px dotted black;
    }
    >.bottom {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
    }
    .loading {
        margin: 10px 0;
        display: flex;
        justify-content: center;
    }
`